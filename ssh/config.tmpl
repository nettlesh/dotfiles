# NOTE: Must come before Host *
Include config.d/*

Host *
    IdentityAgent "{{ '~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock' if os() == 'macos' else '~/.1password/agent.sock' }}"
