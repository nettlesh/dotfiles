[user]
    signingkey = key::ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFOWqnOUZkNfa0mvlobU+pg15gwMi5F4/5Ab9lf9y8vP

[gpg]
    format = ssh

# Use 1Password for SSH key signing
[gpg "ssh"]
    allowedSignersFile = ~/.ssh/allowed_signers
    program            = {{ '/Applications/1Password.app/Contents/MacOS/op-ssh-sign' if os() == 'macos' else '/opt/1Password/op-ssh-sign' }}

[commit]
    gpgsign = true

[tag]
    gpgsign = true
